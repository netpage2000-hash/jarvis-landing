# Hosting

Served via **Cloudflare Pages**, connected to the `netpage2000-hash/jarvis-landing` GitHub repo.

Pushes to `main` trigger an automatic deploy. No build step — Cloudflare serves `index.html` directly.

DNS: `www.jarvis-murphy-ai.com` → Cloudflare Pages (proxied)
